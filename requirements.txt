# ── Core Web Framework ───────────────────────────────────────────
flask==3.0.3
flask-cors==4.0.1
flask-limiter==3.5.0
flask-sqlalchemy==3.1.1
python-dotenv==1.0.1
werkzeug==3.0.3
gunicorn==22.0.0

# ── Authentication ───────────────────────────────────────────────
bcrypt==4.1.3

# ── AI / LLM ─────────────────────────────────────────────────────
groq==0.9.0

# ── Data & ML — Core ─────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.1
scipy==1.13.1

# ── ML — Optional but strongly recommended ───────────────────────
xgboost>=2.0.0
lightgbm>=4.0.0
catboost>=1.2.0
statsmodels>=0.14.0

# ── Forecasting (optional — heavier build, skip on constrained hosts) ──
# Prophet needs a C++ compiler toolchain at install time. On Render/Railway
# free tiers this can fail or slow the build; ARIMA/SARIMA (statsmodels,
# above) cover forecasting without it. Uncomment if you have headroom:
# prophet>=1.1.5

# ── Explainable AI (SHAP) ────────────────────────────────────────
shap>=0.44.0

# ── PDF Generation (REQUIRED for PDF download) ───────────────────
reportlab==4.2.0

# ── Data Viz ─────────────────────────────────────────────────────
matplotlib==3.7.5
seaborn==0.13.2
openpyxl==3.1.5
pillow>=10.0.0  # used directly by report_generator.py to size embedded SHAP images